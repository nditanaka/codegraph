{
  "name": "codegraph",
  "version": "2.1.0",
  "private": true,
  "description": "Codegraph monorepo: run backend API and frontend dashboard together",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm --prefix backend start\" \"npm --prefix frontend run dev\"",
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "test": "npm --prefix backend test && npm --prefix frontend test",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
